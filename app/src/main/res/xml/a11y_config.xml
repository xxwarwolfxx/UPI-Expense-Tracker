<?xml version="1.0" encoding="utf-8"?>
<!--
  packageNames lists every UPI app we read. All four have an ACTIVE confirm-sheet parser in
  ConfirmSheetRegistry.default() (GPay / PhonePe / Paytm / CRED). KEEP THIS LIST IN SYNC with the registry.
  flagReportViewIds keeps getViewIdResourceName() non-null so the debug-only learning mode can anchor a
  harvest dump on stable resource ids, not fragile visible text (RN/Flutter apps often hide the amount in
  a contentDescription / behind a resource id).
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows|flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:canTakeScreenshot="false"
    android:description="@string/a11y_desc"
    android:notificationTimeout="100"
    android:packageNames="com.google.android.apps.nbu.paisa.user,com.phonepe.app,net.one97.paytm,com.dreamplug.androidapp" />
