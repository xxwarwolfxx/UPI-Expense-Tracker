<?xml version="1.0" encoding="utf-8"?>
<!-- Quota meter. Default 2x1 (the size it was designed at); resizes wider, where the labels lengthen. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="110dp"
    android:minResizeWidth="100dp"
    android:minResizeHeight="100dp"
    android:targetCellWidth="2"
    android:targetCellHeight="1"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:initialLayout="@layout/widget_quota"
    android:previewLayout="@layout/widget_quota"
    android:description="@string/widget_quota_desc"
    android:updatePeriodMillis="0" />
